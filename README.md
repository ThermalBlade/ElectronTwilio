# ElectronTwilio

To get dependencies in folder, first stab your eyes with a fork... then:
1. npm init
2. npm install --save electron
3. npm install electron
4. npm install electron-builder --save-dev

There are other dependencies in this project, it will yell at you to install those too.

"npm run dist" to create the app